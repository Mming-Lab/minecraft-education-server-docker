# ================================================
# ボリューム設定
# ================================================

VOLUMES_BASE_PATH=./
# ボリュームのベースパス（末尾に / を付ける）
# ローカル開発環境: ./
# NASやリモートストレージ: /share/Public/minecraft-edu/
# 使用可能な値: 相対パス（./）または絶対パス（/path/to/...）

# ================================================
# サーバー基本設定
# ================================================

SERVER_PUBLIC_IP=
# サーバーの公開IPアドレスまたはドメイン名（必須設定）
# 同じLAN内（クライアントとサーバーが同じマシンの場合も含む）: DockerホストのプライベートIPアドレス（例: 192.168.1.100）
# インターネット経由の場合: グローバルIPアドレスまたはドメイン名（例: 203.0.113.1, your-server.example.com）
# 注意: DockerコンテナとしてサーバーをホストしているマシンのIPアドレスを指定してください
# 使用可能な値: ポート番号を含まない有効なIPアドレスまたはドメイン名

# ================================================
# 共通設定（全ワールドのデフォルト値）
# ================================================
# 複数ワールド運用時、すべてのワールドに適用されるデフォルト値です。
# 個別のワールド設定（_WORLD1、_WORLD2など）で上書き可能です。

GAMEMODE_COMMON=creative
# ゲームモード
# 使用可能な値: "survival"（サバイバル）, "creative"（クリエイティブ）, "adventure"（アドベンチャー）

DIFFICULTY_COMMON=easy
# 難易度
# 使用可能な値: "peaceful"（ピースフル）, "easy"（イージー）, "normal"（ノーマル）, "hard"（ハード）

MAX_PLAYERS_COMMON=10
# 最大プレイヤー数
# 使用可能な値: 任意の正の整数

DEFAULT_PLAYER_PERMISSION_LEVEL_COMMON=member
# 新規プレイヤーの権限レベル
# 注意: この設定はワールド作成時に確定します。後から変更しても既存ワールドには反映されません。
#       変更するにはワールドデータ（worlds/world{N}/）を削除して再作成する必要があります。
# 注意: 教師アカウント（Faculty ライセンス）は常にオペレーターとして参加し、権限を自由に変更できます。
# 使用可能な値: "visitor"（訪問者）, "member"（メンバー）, "operator"（オペレーター）

ALLOW_COMMANDS_COMMON=true
# コマンド（MakeCode、スラッシュコマンド等）の使用許可
# falseの場合、コマンドはサーバーコンソールのみ使用可能（クライアントからは使用不可）
# 注意: MakeCode を使用するには true にする必要があります
# 使用可能な値: "true"または"false"

ALLOW_LIST_COMMON=false
# 接続制限（ホワイトリスト）
# trueの場合、allowlist.json に登録されたプレイヤーのみ接続可能
# 使用可能な値: "true"または"false"

CHAT_RESTRICTION_COMMON=None
# チャット制限レベル
# "None": 通常の自由なチャット
# "Dropped": チャットメッセージは破棄。プレイヤーに無効である旨が通知される。MakeCode は引き続き使用可能
# "Disabled": オペレーター以外はチャットUIが非表示。MakeCode は使用可能だが、チャット経由の入出力は不可
# 使用可能な値: "None", "Dropped", "Disabled"

VIEW_DISTANCE_COMMON=32
# 表示距離（チャンク単位）
# 使用可能な値: 5以上の正の整数

TICK_DISTANCE_COMMON=4
# ティック距離（プレイヤーから何チャンク離れた範囲まで更新されるか）
# 使用可能な値: 4～12の整数

PLAYER_IDLE_TIMEOUT_COMMON=30
# アイドルタイムアウト（分）。この時間アイドル状態のプレイヤーはキックされる
# 0に設定するとプレイヤーは無期限にアイドル状態でいられます
# 使用可能な値: 任意の非負整数

MAX_THREADS_COMMON=8
# サーバーが使用する最大スレッド数
# 0に設定するか削除すると、可能な限り多くのスレッドを使用します
# 使用可能な値: 任意の正の整数

TEXTUREPACK_REQUIRED_COMMON=true
# テクスチャパックの強制適用
# 使用可能な値: "true"または"false"

CONTENT_LOG_FILE_ENABLED_COMMON=false
# コンテンツエラーのファイルログ記録
# 使用可能な値: "true"または"false"

DISABLE_PLAYER_INTERACTION_COMMON=false
# プレイヤー同士の直接攻撃・すり抜けの制御
# trueの場合、プレイヤー同士がすり抜け可能になり、直接攻撃できなくなります
# ブロック設置/破壊、TNT爆発などの間接的な影響は引き続き有効です
# 使用可能な値: "true"または"false"

DISABLE_CUSTOM_SKINS_COMMON=false
# カスタムスキンの無効化
# trueの場合、Minecraft外でカスタマイズされたプレイヤースキンを無効にします
# 使用可能な値: "true"または"false"

LEVEL_NAME_COMMON=Education level
# ワールド名（ゲーム内で表示される名前）
# 使用可能な値: 任意の文字列

# ================================================
# ワールド1設定（個別設定）
# ================================================
# ワールド1専用の設定です。
# 省略した項目は上記の共通設定（_COMMON）が使用されます。

SERVER_PORT_WORLD_1=
# ワールド1のIPv4ポート番号（必須 - 起動前に設定してください）
# 注意: 教育版はIPv6に非対応です
# 複数ワールド運用時は、各ワールドで異なるポート番号を設定してください
# 例: SERVER_PORT_WORLD_1=19132, SERVER_PORT_WORLD_2=19134
# 使用可能な値: 1～65535の整数

# ワールド1のカスタマイズ例（必要に応じてコメント解除）
# ALLOW_COMMANDS_WORLD_1=true
# LEVEL_NAME_WORLD_1=World 1
# GAMEMODE_WORLD_1=creative
# DIFFICULTY_WORLD_1=easy
# LEVEL_SEED_WORLD_1=12345
# 注意: LEVEL_SEED はワールド生成時のみ有効。生成済みのワールドには影響しません

# ================================================
# ワールド追加用
# ================================================
# ワールドを追加する場合:
#   1. docker-compose.world{N}.yml.example をコピー（例: docker-compose.world2.yml）
#   2. ファイル内の {N} を番号に置換（例: 2）
#   3. 以下のポート設定を追加
#   4. 起動: docker compose -f docker-compose.yml -f docker-compose.world2.yml up -d
#
# SERVER_PORT_WORLD_2=19134
# # その他の設定は共通設定を継承（必要に応じて個別設定を追加）
# # LEVEL_NAME_WORLD_2=World 2
# # GAMEMODE_WORLD_2=adventure

# ================================================
# 環境変数の優先順位（3段階フォールバック）
# ================================================
# 1. ワールド個別設定 (例: GAMEMODE_WORLD_1) ← 最優先
# 2. 共通設定 (例: GAMEMODE_COMMON)
# 3. デフォルト値 (docker-compose.yml内で定義)
#
# 例: GAMEMODE_WORLD_1が未設定 → GAMEMODE_COMMONを使用
#     GAMEMODE_COMMONも未設定 → デフォルト値 "creative" を使用
